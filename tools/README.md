# Tools

The `tools/` directory contains standalone interactive study utilities.

- `pipeline-simulator.jsx`: visual practice for instruction flow, hazards, and stalls
- `formula-brain.jsx`: formula review, recall practice, and exam-style reinforcement

These files complement the React application in `pipeline-site/` and can also be maintained independently.
